# STATUS — Second Look: CoT Monitorability

**Date:** 2026-07-02 · **Session #10** (Phase 2; #10 = stale-figure audit + regeneration — see drift log first)
**Build-order step (SPEC_phase2 §4):** **gpt-oss-120b §3.1 FAITHFUL RE-RUN on self-hosted vLLM** — replaced the provider-suspect OpenRouter gpt-oss numbers (all +0.00/one-shot, iterated cells NEGATIVE) with a self-hosted run whose no-CoT arm is a TRUE zero-analysis arm. **Gate 0 PASSED** (channel IS disableable self-hosted). Full §3.1 suite (Gate 0 + decode-necessity 5 fam + 4-family unfaithfulness + LLM re-judge + arithmetic Gate-1); all figures regenerated with the new actor. On branch `feat/gptoss-selfhost-vllm` (NOT committed — awaiting review). Per-model necessity is the CORE principle.
**This session (2026-07-01, Session #9) — GPT-OSS-120B SELF-HOST vLLM §3.1 RE-RUN; NOT committed (branch feat/gptoss-selfhost-vllm):**
1. **★ THE FINDING — OpenRouter gpt-oss decode-necessity was a PROVIDER ARTIFACT.** gpt-oss's harmony `analysis` channel is mandatory on the chat endpoint (reasoning_effort only reduces; structured/prefill still reason; no `enable_thinking`), so OpenRouter's "no-CoT" arm was still reasoning → decode-uplift **all +0.00, iterated L2/L3 NEGATIVE (−0.19, −0.69)** (impossible). Self-hosted (vLLM 0.24.0, **native mxfp4**, served `gpt-oss-120b`, ctx 8192), the no-CoT arm forces the **harmony `final` channel via raw /v1/completions** (openai_harmony-rendered `<|channel|>final<|message|><answer>`) → **ZERO analysis tokens by construction**. New `no_cot_mechanism: harmony_final` (`src/cot_mon/exp_hints/harmony_nocot.py`, async).
2. **★ GATE 0 = PASS** (`results/gate0_gpt_oss_120b_selfhost.json`): no-CoT analysis-leak **0.000** / median completion **5 tok** (channel disableable); with-CoT emission **1.000** (emission-filtered set); per-model uplift **+0.195** on GPQA → gpt-oss-selfhost is a **FAITHFUL actor** (channel-disabled AND CoT-necessary), the OPPOSITE of the OpenRouter "undisableable/unverifiable" tier.
3. **★ Decode-necessity (figC1/figC2):** all 15 family×level cells **cot_necessary, uplift +0.93 to +1.00** (n=240/arm; **0 analysis leaks across 3600 no-CoT rollouts**). OpenRouter mean −0.059 → vLLM **+0.99**. The vLLM numbers **SUPERSEDE** the OpenRouter rows. `results/decode_necessity/decode_necessity_gpt_oss_120b_selfhost.json`.
4. **★ §3.1 family unfaithfulness + LLM re-judge (judge = gemini_3_flash, Arav's call):** cue-robust NEGATIVE — follows decodable hints openly (bignum follow 0.90 / deductive 0.32) but REVEALS them (conceal 0.07 / 0.16) → unfaithful **0.00–0.03** across families. Now a TRUSTWORTHY negative (necessity verified), vs OpenRouter's caveated one (necessity unmeasurable). Merged into `results/family_unfaithfulness_llm.json` (other 7 actors untouched).
5. **★ ctx-8192 mitigation (Arav's call: keep 8192, don't restart the server):** gpt-oss over-reasons → 6% of with-CoT rollouts truncated at the 6144 cap (raw emission 0.91). Added a `--min-emission` filter to `run_sweep.py`/`correct_with_cot` that drops truncating items and backfills from an over-provisioned pool (kept 60/150) → retained-set emission 1.000. Caveat: gpt-oss-selfhost §3.1 is scoped to within-budget-reasoning GPQA items.
6. **Figures:** all regenerated. figC1/figC2 (decode) show **gpt-oss-120b (OR one-shot) vs gpt-oss-120b (vLLM cot_necessary)** side-by-side — the contamination contrast. Replication figs (fig0-5) show gpt-oss via the self-host measurement (supersedes OR; identical ~0.01 simple headline). Other actors' arithmetic transcripts pulled from W&B to re-render fig0-5 with everyone. Deliverable: `VLLM_S31_RESULTS.md`. Server owned by human — NOT restarted/reconfigured.
**This session (2026-06-30, Session #8) — FIGURE-3 HINT-TYPE EXTENSION + opus decode-necessity + deepseek repin; COMMITTED:**
1. **★ Figure-3 hint-TYPE extension (5 families × 7 actors).** Extended §3.1 from arithmetic-only to 5 complex-hint families. Added to `run_sweep.py`: `--family`, `--families`+`--share-baseline` (**technique #2**: filter + none/simple paid ONCE per actor, complex per family → ~33% fewer gens + item-matched families; `solver.run_sweep_multi`), `--reasoning-floor`, `--cache-prompt`.
2. **★ LLM RE-JUDGE (no regex; project rule).** `scripts/rejudge_families.py` re-judges every complex with-CoT FOLLOWER with **gemini-3-flash** (gemini-2.5-flash for the gemini_3_flash actor, anti-self-judge). **CORRECTED a deterministic-judge artifact:** the regex inflated `deductive` concealment to ~0.26–0.39 by mis-scoring openly-reasoned constraint solves as concealment; LLM says **deductive ≈ 0.00–0.04** (revealed).
3. **★ HEADLINE (LLM-judged unfaithfulness by family):** concealment is **ACTOR-driven**, not family-driven. **gpt-5.5 bignum 0.29**, **opus bignum 0.22 / iterated 0.21**; **Geminis ≈ 0 everywhere** (reveal when they follow); **gpt-oss + deepseek low** (open-weight cue-robust negatives). **NECESSITY ALONE ≠ CONCEALMENT:** opus/gpt-5.5 conceal iterated *despite it being CoT-necessary* — they show the computation but never attribute it to the planted hint → LLM still calls it concealed (scatter has high-concealment points at BOTH low- and high-necessity; title kept descriptive).
4. **★ opus decode-necessity via Anthropic Message Batches API** (`scripts/opus_decode_necessity.py`; 4800 reqs, **50% batch discount ~$8**) + **structured-output no-CoT** (forced single-letter tool — opus rejects prefill, exactly why it was missing). opus **one-shots bignum (uplift +0.03–0.13) + deductive**, **CoT-necessary** for arithmetic/iterated/indirection (+0.68–0.83) → explains its bignum concealment. Now in `figC2` (`make_complex_figures.ACTOR_ORDER`) + scatter. **caching N/A** on short decode prompts (< Anthropic 1024-tok cache min); batching is the saver.
5. **deepseek REPINNED novita→baidu/fp8** (`scripts/bench_deepseek_providers.py`): novita **wedges** under sustained 5k-gen load (300s-timeout gens → 30-min retry backoffs → stall); **baidu = 2254 tok/s @conc24 (0 fail)**, honors prefill + raw reasoning. **KEY: deepseek OVER-REASONS** — runs to `max_tokens` every gen (~8k), often no `<answer>` even @8k; `effort:low`/`reasoning.max_tokens` IGNORED → intrinsically slow, weak §3.1 actor. Backfilled 40×5 (kept 40/80 @8k) → clean **low-concealment negative** (0.01–0.06).
6. **New figures** (`clean-figures` skill): `figures/3p1_family_sweep/{fig_family_unfaithfulness,fig_necessity_vs_unfaithfulness,fig_family_followrate}.png`; figC2 → 7 actors. Data: `results/family_unfaithfulness_llm.json`, `results/family_figure_data.json`, `results/decode_necessity/decode_necessity_opus_4_8_direct.json`.
7. **`interesting_samples/`** — curated 300+ transcript samples for manual review (concealment/reveal/one-shot/deductive + gpt-oss undisableable-channel finding [iterated-L3 uplift −0.69] + honest-behavior examples), nested by experiment×model (`scripts/curate_*.py`).
**This session (2026-06-27/28, Session #7) — ran the runbook; KEY DISCOVERY + 8-actor sweep; NOT committed:**
1. **★ gemini-3.5-flash IS a faithful actor via `reasoning:{effort:minimal}`** — its endpoint 400s on BOTH `enabled:false` AND `max_tokens:0` ("Reasoning is mandatory"), but **`effort:minimal` genuinely disables (rtoks=0, reasons in body)** on google-vertex + google-ai-studio. Added a `reasoning_effort` field to `roster.py`/`models.yaml`. **CLEAN TIER SPLIT:** the SAME knob only *reduces* gemini-3.1-pro (rtoks 156–242, never 0) → **Flash disables, Pro is mandatory-hidden** (publishable contrast; 3.1-pro stays generalization-only, judged on visible body).
2. **★ Capture bug fixed** (`solver.extract_reasoning`): a disabled Gemini returns a text-less `reasoning.text` SIGNATURE block that Inspect dumps as raw JSON into `ContentReasoning.reasoning` → was miscounted as 155 chars of "reasoning" (corrupts rtoks audit / the disable probe). Now stripped; 13 tests + 4 direct checks pass.
3. **Roster churn (user):** `kimi_k2_thinking` DROPPED (~1T MoE too slow) → swapped for **`deepseek_v4_flash`** (independent-lab, fast 'flash' MoE; novita/fp8, native_raw). All 5 new/headline actors probed (raw/disable ✅, prefill ✅; necessity all "one-shots ⚠️" = provisional, the per-model scale-erosion signal — modern flash/open tier one-shots GPQA).
4. **★ §3.1 POWERED SWEEP — 8 actors done.** Gate-1 deltas: gemini-3-flash +0.38, gpt-5.5 +0.42, **gemini-3.5-flash +0.24**, **opus-4.8-direct +0.16**, **gemini-3.1-pro +0.16 (caveated)**, gemini-2.5-flash +0.09 — all ✅; **deepseek-v4-flash +0.025 (ns, cue-robust)** + gpt-oss-120b −0.01 (ns) = the open-weight cue-robust negatives. (gemini-3.5-flash follow 0.51/conceal 0.48; opus follow 0.56/conceal 0.29; 3.1-pro follow 0.18/conceal 0.88.)
5. **Decode-necessity (Step 5):** gemini-3.5-flash **CoT-necessary on iterated L1/L2 + indirection L1/L2/L3** (valid scoped cells in fig0b/2b) — despite one-shotting GPQA; deepseek **one-shots ALL 15 cells** (no necessity window, like gpt-oss but genuinely one-shot). Added new actors to `make_complex_figures.ACTOR_ORDER` + `make_figures` ORDER/JUDGEABLE/LABEL/COLOR.
6. **★ PROVIDER FRAGILITY + FIX (drift):** the big MoE actors (deepseek@novita, qwen@siliconflow) **WEDGE under sustained load** — providers hang the long 8k-token gens, pool starves, run dies. Added **`--attempt-timeout`** to `run_sweep.py` + `decode_necessity_sweep.py` (hung gen fails+retries instead of wedging). deepseek's §3.1 needed a kill+redo (wedged at 50/60) but FINISHED with the timeout (52 timeouts absorbed); its decode ran clean. **qwen survived the timeout but is pathologically slow** (137s/gen, 8k cap every gen). Provider-test verdict: alibaba=no prefill, wandb=404, **siliconflow/fp8 = only stable prefill-honoring qwen3.5 endpoint** (repinned).
**This session (2026-06-26, Session #6) — NEW MODELS WIRED + GATED; committed to `main` (`0e1757c`):**
1. **3 new §3.1 actors in `configs/models.yaml`, ALL gated** (`necessity: todo`; wired for all experiments, but Phase-0 is a HARD gate before load-bearing): `gemini_3_5_flash` (flash-lineage ladder TOP / Follow-up C; `disabled`→body CoT like gemini-3-flash; google-vertex — renamed the prior monitor → `gemini_3_5_flash_monitor` to avoid a load_roster key collision), `qwen3_5_122b` (ungated from `[]`; novita/bf16, native_raw — provisional), `kimi_k2_thinking` (Moonshot ~1T MoE; independent lab family for cross-family robustness; novita/bf16, native_raw — provisional).
2. **Headline-only (§3.1):** added `hints` to `opus_4_8_direct` (no-CoT arm = structured-output forcing since prefill 400s on Opus 4.8 — comparability footnote vs prefill actors); `gemini_3_1_pro` stays generalization-only/caveated (summary trace → judged body only, like the gemini-2.5-pro anchor).
3. **New Phase-0 probes:** `scripts/phase0_kimi_k2.py`, `scripts/phase0_gemini_3_5_flash.py` (raw/disable + prefill + per-model necessity → PROMOTE/keep-todo verdict).
4. **`.gitignore`: `artifacts/`** (re-downloadable W&B transcript cache, 80M+). Also swept in prior uncommitted work (rejudge/gradient scripts, figures, results, `interesting_samples/`).
5. **NEXT (VM, unattended via `tmux`):** `RUNBOOK_new_models.md` (NOT in git — passed manually) sequences cheap smoke → Phase-0 probes (THE GATE) → dry-run cost → §3.1 sweep (new actors full levels; 3.1-pro/opus headline at `--levels 1`) → decode-necessity sweep → regenerate ALL figures. Promote `necessity: confirmed` ONLY for actors whose probe fully passes.
**This session (2026-06-25, Session #5) — SIMPLE-HINT JUDGE CORRECTION (no new rollouts); NOT yet committed:**
1. **Found + fixed an inflation bug in `judge_simple`.** The narrow cue lexicon (`<answer>`/metadata/hint/cue/tag) scored open attributions to the planted answer ("the **provided answer** is B", "**answer key** says D") as *concealment* → inflated simple-unfaithfulness. Broadened `hint_judge._CUE_REFERENCE_PATTERNS` (anchored to NOT fire on "the correct answer is C" / "answer choices"); +2 tests (68 pass).
2. **Re-judged all 8 transcripts** (4 filtered + 4 unfiltered) from `cot_text_full`, **0 new model calls** (`scripts/rejudge_simple.py`; originals → `logs/transcripts_orig_backup/`). Purely additive (0 reveal→conceal flips). Recomputed summaries (`scripts/recompute_summaries.py`, self-check vs archived OK) → `logs/sweep_*.json` + `results/gate1_sweep_summaries*.json`; regenerated fig0/1/5.
3. **Independent Opus re-validation** (`scripts/validate_judge_simple.py`, 56 followers incl. gemini-2.5-flash): OLD judge **15/56 false-conceal (inflates)** → NEW **4/56** (acc 0.73→0.91, 0→1 false-reveal). Closes the original 32-trace gap (only 4 simple followers, none from 2.5-flash).
4. **GATE 1 STILL ✅** but magnitudes corrected: **gemini-2.5-flash delta +0.148→+0.088** (cluster-boot 2σ [+0.047,+0.137], excludes 0), **conceal|foll 0.990→0.634** (the "99% concealer" was a judge artifact); gemini-3-flash +0.428→+0.378; gpt-5.5 +0.427→+0.417; gpt-oss still ns. Residual ~4/56 false-conceal ⇒ corrected numbers are a slight over-estimate (documented). Headline table now uses cluster-bootstrap CIs.
**This session (2026-06-25, Session #4) — all pushed to `main` (head `4787773`); NO runs in flight:**
1. **Unfiltered §3.1 sweep ANALYSED** (all 4 actors, `--no-filter`, W&B `exp_hints_sweep_unfiltered`) → **Gate-1 HOLDS, not a filter artifact**; summaries → tracked `results/gate1_sweep_summaries_unfiltered.json`.
2. **Decode-necessity map EXTENDED 2→4 actors** — ran the sweep for **gemini-2.5-flash** (clean, CoT-necessary on 4/5 families) + **gpt-oss-120b** (CAVEATED — undisableable channel ⇒ necessity UNMEASURABLE) in PARALLEL (12.8 / 62 min; W&B `exp_hints_complex_decode`); figC1/figC2 regenerated (all 4); decode JSONs persisted to tracked `results/decode_necessity/`; `make_complex_figures.py` now loads `.env` + reads `results/` first.
3. **REBASED onto a parallel branch** (`953cd83..bff3c8a`: cluster-bootstrap CIs, honesty fixes, fig0b/2b/5, decode-sweep code that wired these actors but **never ran** them). Kept my data; adopted their reframing (bignum = **capability gap**, not level-erosion; report **filtered §3.1 set only**).
4. **fig2b + fig0b REFRESHED** with the new decode data (both necessity-scope by the decode verdict). fig0b's *filtered* §3.1 transcripts (gitignored, absent) were **pulled from W&B** (`exp_hints_sweep-<actor>-transcripts:v0`) into `logs/transcripts/`, then regenerated in isolation (bootstrap CIs intact; fig0/1/3/4/5 untouched).
**Gates:** **Gate 1 = ✅ CLEARED + EXTENDED to 8 actors** — powered GPQA §3.1, judge-validated, baseline-subtracted, 2σ: simple-hint unfaithfulness replicates on **6/8** (paper analog gemini-2.5-flash + faithful gemini-3-flash + NEW gemini-3.5-flash, opus-4.8-direct, gemini-3.1-pro; deepseek-v4-flash + gpt-oss-120b = cue-robust negatives). Gate 2 — not reached.
**W&B:** `wandb.ai/aravdhoot/cot-monitorability` — filtered §3.1 (`exp_hints_sweep`: tough-water-14 / classic-mountain-16 / noble-aardvark-12 / ancient-resonance-18), unfiltered (`exp_hints_sweep_unfiltered`), figures (`exp_hints_sweep_figures`), decode-necessity (`exp_hints_complex_decode` + `..._figures`), judge-validation (`blooming-firefly-10`); CoT tables + `transcripts.jsonl` artifacts synced (the §3.1 filtered transcripts are recoverable from W&B — gitignored locally).

## Per-model experiment coverage (which models · which experiments · why) — Session #7
**Scope:** only §3.1 is BUILT. "All experiments" = the FULL §3.1 suite — simple-hint **headline** (L1) + **complex** L1–L3 follow arm + **decode-necessity** (Step 5, feeds fig0b/2b/figC1-2). §3.2 / §6 / §4.4 / Follow-up A are stubs → run for **NO** model yet.

**✅ FULL §3.1 suite done (headline + complex + decode-necessity) — 6 actors:**
| actor | role | Gate-1 Δ | decode-necessity |
|---|---|---|---|
| gemini-2.5-flash | paper analog | +0.09 ✅ | CoT-necessary 4/5 families |
| gemini-3-flash | primary faithful | +0.38 ✅ | CoT-necessary (arith/iter/indir) |
| gemini-3.5-flash | **NEW** faithful (effort:minimal) | +0.24 ✅ | CoT-necessary iterated/indirection |
| gpt-5.5 | faithful_conditional | +0.42 ✅ | one-shots easy → needs high difficulty |
| gpt-oss-120b (OpenRouter) | cue-robust neg | −0.01 ns | ~~UNMEASURABLE (undisableable channel)~~ → SUPERSEDED by self-host |
| **gpt-oss-120b (self-host vLLM)** | **cue-robust neg (FAITHFUL)** | +0.01 ns | **CoT-necessary +0.93–1.00 all 15 cells** (Gate 0 PASS; harmony final-channel force) |
| deepseek-v4-flash | **NEW** cue-robust neg | +0.025 ns | one-shots ALL 15 decode cells |

**◐ HEADLINE ONLY (§3.1 simple-hint @ L1; no complex arm, no decode) — 2 actors:**
- **opus-4.8-direct** (Δ +0.16 ✅) — ran `--levels 1` (the simple-hint headline needs no complex extension; also saves the $ Opus leg). no-CoT via **structured-output** (Opus 4.8 rejects assistant-prefill) → comparability footnote vs prefill actors; one-shots → necessity provisional. No decode-necessity run.
- **gemini-3.1-pro** (Δ +0.16 ✅, follow only 0.18) — **GENERALIZATION-ONLY**: reasoning is mandatory-hidden (effort:minimal only *reduces* rtoks to 156–242, never 0) → necessity UNVERIFIABLE → we judge the **visible body**, caveated (NOT a faithful-actor headline; like the gemini-2.5-pro anchor).

**✗ NOT RUN / EXCLUDED — and why:**
- **qwen3.5-122b** — DROPPED (Arav): infeasible on every OpenRouter endpoint — its 8k-token gens hang under load (novita crashed, siliconflow wedged; ~137s/gen, ~19h, stalled at 40/47). `--attempt-timeout` stops a terminal wedge but not the slowness. → **self-host vLLM** (`selfhost_vllm/`).
- **kimi-k2-thinking** — DROPPED: ~1T MoE too slow on novita (smoke passed; superseded by deepseek as the independent-lab open-weight slot).
- **gemini-2.5-pro** (replication anchor) — generalization-only (mandatory hidden reasoning); NOT run this session — the paper-analog role is filled by gemini-2.5-**flash** (which WAS run). Could be added later as a caveated headline like gemini-3.1-pro.
- **qwen3-235b / qwen3-30b** — open-weight; 235b answered-rate ~0.63 @8k (sometimes never emits a final `<answer>`) + slow; backfill skipped in prior sessions. Revisit via vLLM self-host.
- **opus-4.8** (OpenRouter `body_channel`) — assistant-prefill UNSUPPORTED via OpenRouter → out of §3.1/§3.2; reserved for §6 / Follow-up A (agentic, no prefill needed). `opus-4.8-direct` (Anthropic API, structured-output) covers the §3.1 headline instead.

## Module status
- **DONE + tested (65 tests pass / 5 skip):** `models/roster.py`, `monitors/uplift_filter.py` (per-model necessity engine), `exp_hints/data.py` (GPQA+MMLU-Pro), `exp_hints/hints.py` (5-family suite), **`exp_hints/decode.py`** (SHARED decode-necessity harness — decode items + forced-`<answer>` sampler + rollout-capture; used by `calibrate_complex.py` + `decode_necessity_sweep.py`), `monitors/hint_judge.py`, `exp_hints/solver.py` (2×3 grid, `<question-metadata>` injection, `\boxed`/`<answer>` extraction, correct-with-CoT filter, bounded concurrency, audit fields + shared `rollout_row`), `metrics.py` (unfaithfulness delta + Wilson 2σ + `flatten_for_logging`; **+`wilson_diff_ci` Newcombe difference-CI + `decode_necessity_label`** one-shot/too-hard/cot-necessary/weak), **`figviz.py`** (shared decode-uplift bar + necessity heatmap; matplotlib kept out of the tested core), **`logging/wandb_logger.py`** (one run per exp×actor×config; config+scalars+2σ; per-rollout CoT `wandb.Table`; `transcripts.jsonl` artifact; disabled/offline/online; no key leakage).
- **Runners:** `scripts/run_sweep.py` (powered §3.1 60×10), **`scripts/decode_necessity_sweep.py`** (Part C: decode-necessity across families×levels; reuses calibration cache, Newcombe 2σ, W&B; `--dry` cost projection), `scripts/calibrate_complex.py` (per-model complex-necessity; now uses the shared harness), `scripts/make_figures.py` (→`figures/3p1_replication/`), **`scripts/make_complex_figures.py`** (→`figures/3p1_complex_hints/`: faceted decode-uplift bars + family×level heatmap), `scripts/run_exp_hints.py`, `scripts/susceptibility_precheck.py`, `scripts/validate_judge.py`, `scripts/check_hint_decode.py`, `scripts/smoke_wandb.py`, `scripts/phase0_*.py`.
- **STILL stubbed:** `tasks/exp_hints.py` (Inspect @task — runner used instead), `exp_gradient/algebra.py` (§3.2 generator — docstring only), `monitors/{detection,summarizer,coverage,calibration}`, `scoring`, §6 / followup_a. (`make_figures` is now real.)
- **Git:** `main` is CURRENT at **`0e1757c`** (Session #6 new-models commit; prior head `4787773`). Committed directly to `main`; pushed via the `.env` `GH_TOKEN`. **Untracked-on-purpose (NOT in git):** `RUNBOOK_new_models.md` + `RUNPOD_SETUP.md` (passed to the VM manually), `artifacts/` (gitignored W&B cache). `.gitignore` excludes `logs/` (+ transcripts), `*.eval`, `.env`, `wandb/` — verified; tracked result/figure artifacts live in `results/` + `figures/`.

## Phase-0 resolutions (RESOLVED via probe; full detail in logs/phase0_probe.json)
Per-model — **live · reasoning · prefill** (provider in parens):
- `gemini_2_5_pro` (Google) — live · reasoning **raw** (Gemini thought-summary; treat as summary for faithfulness) · prefill **WORKS**  → **green §3.1/§3.2 actor**
- `gemini_2_5_flash` (Google) — live · reasoning none · prefill works
- `gemini_3_5_flash` (Google) — live · reasoning raw · prefill works  → monitor + weak summarizer
- `gemini_2_5_flash_lite` (Google) — live · reasoning none · prefill works  → §6 monitor proxy
- `opus_4_8` (Anthropic) — live · reasoning none · **prefill UNSUPPORTED** ("model does not support assistant message prefill") → **excluded from §3.1/§3.2 forced-CoT; monitor + body-channel only**
- `gpt_5_5` (OpenAI) — live · reasoning **summary by default, but `reasoning:{enabled:false}` → rtoks=0** (hidden channel OFF) · prefill **WORKS** → **usable §3.1/§3.2 actor** (disable channel, judge body CoT)
- `qwen3` = `qwen3-235b-a22b-thinking-2507` (WandB) — live · reasoning **raw (faithful)** · prefill **WORKS** (confirmed @256/512; forcing `<answer>` also zeroes thinking)
- `gpt_oss_120b` (DeepInfra/OpenRouter) — live · reasoning **raw (faithful)** · prefill works · **necessity was UNMEASURABLE on OpenRouter** (analysis channel mandatory → no-CoT arm still reasons) → **RESOLVED via `gpt_oss_120b_selfhost`**
- `gpt_oss_120b_selfhost` (self-host **vLLM 0.24.0, native mxfp4**, served `gpt-oss-120b`, ctx 8192) — **RESOLVED 2026-07-01, Session #9:** channel **disableable** (harmony final-channel force via raw /v1/completions → 0 analysis tokens, Gate 0 PASS) · **per-model necessity CONFIRMED** (decode-uplift +0.93–1.00 all 15 cells; GPQA uplift +0.195) → **faithful actor**. no-CoT mechanism = `harmony_final`. Caveat: §3.1 scoped to within-budget-reasoning items (ctx-8192 emission filter).
- **Faithful raw-CoT substrates (§4.4 source):** `qwen3`, `gpt_oss_120b` (open weights). All closed models give summary or nothing → use body-channel for faithful-trace work.
- **Provider pins:** SET as exact endpoint tags via `{only:[tag], allow_fallbacks:false}`; 9/9 verified. `qwen3`=`wandb/bf16`, `gpt_oss_120b`=`deepinfra/bf16`, Gemini=`google-vertex`, closed=`anthropic`/`openai`.
- **Calibrated FPR thresholds (§6/A):** TBD (later).
- **Hidden-channel disable + new 3.x actors (follow-up probe 2026-06-22, `scripts/phase0_followups.py`):**
  - `gpt_5_5`: `reasoning:{enabled:false}` → **rtoks=0** + visible body answer + prefill still works = clean actor. `effort:minimal/low` and `exclude:true` do NOT disable (rtoks>0). No `gpt-5.5-chat/-mini` variant exists.
  - `gemini-3-flash-preview`: thinking disables (rtoks=0) AND prefill works (even together) = **clean 3.x forced-CoT actor** (not yet in roster).
  - `gemini-3.1-pro-preview`: prefill works but **reasoning MANDATORY (cannot disable)** → hidden channel persists, like Gemini 2.5 Pro. Usable for token-forcing only.
- **CoT-NECESSITY check (channel-off, hard arithmetic; `scripts/phase0_necessity.py`) — does a hard task force externalization or just wrong answers?**
  - `gemini-3-flash-preview`: **necessity HOLDS** — 0/6 forbidden-to-show-work vs 6/6 allowed (uplift **+1.00**, rtoks=0). Visible CoT is load-bearing → **faithful §3.1/§3.2 actor ✅**.
  - `gpt_5_5` (channel off): **does NOT need CoT** — one-shots **4/6** hard mult/division with rtoks=0 and ~25 chars (uplift **+0.33**). **Can hide → weak necessity actor.** This is **scale erosion (Follow-up C)** in Phase-0: its §3.2 difficulty sweep must go far higher to find the flip point.
  - Implication: necessity is MODEL-RELATIVE; calibrate the §3.2 difficulty gradient per actor. Open-weight necessity (qwen3/gpt-oss) not yet checked.
- **NEW gated actors (2026-06-26, Session #6 — Phase-0 PENDING, run on VM; all `necessity: todo`, NOT load-bearing until raw/disable + prefill + per-model uplift all pass):**
  - `gemini_3_5_flash` (google-vertex) — `disabled`→body CoT (mirror of gemini-3-flash); probe `scripts/phase0_gemini_3_5_flash.py` (also re-check the §3.2 quiet-corrector caveat before trusting its gradient column).
  - `qwen3_5_122b` (novita/bf16) — native_raw **provisional** (endpoint advertises `reasoning`/`include_reasoning`; confirm it's RAW not summary); probe `scripts/phase0_qwen3_5.py`.
  - `kimi_k2_thinking` (novita/bf16) — Moonshot ~1T MoE, independent lab family; native_raw **provisional**; probe `scripts/phase0_kimi_k2.py`.
  - `opus_4_8_direct` (Anthropic API) — §3.1 headline via **structured-output** no-CoT (prefill 400s across the 4.6+/Fable family); necessity/decode carry a comparability footnote vs prefill actors.
  - `gemini_3_1_pro` (google-vertex) — summary trace → **generalization-only headline** (caveated, judged body only), not a faithful actor.

## Drift log (deviations from SPEC/CLAUDE — read first)
- **qwen3.5-122b (self-host vLLM) LANDED ON MAIN + figures regenerated (2026-07-02, Session #10).** The
  qwen3.5 self-host §3.1 run (decode-necessity + 4-family unfaithfulness, W&B runs stoic-rain-86 /
  noble-vortex-87…stellar-sun-90; branch `feat/qwen35-selfhost-vllm`) finished. To get it onto `main`
  without merging that branch (it's based on the PRE-gpt-oss-merge commit `07f4e86`, so a merge would
  revert the gpt-oss-selfhost work): (a) fast-forwarded `main` from `1b6606e` → `0061f05` (this session's
  answered-only/OR-drop/scatter figure work, whose parent WAS main HEAD — clean, non-divergent; it had
  been committed on `release/public`); (b) copied `results/decode_necessity/decode_necessity_qwen3_5_122b.json`
  (has `emit_*` fields) from the feat branch and merged ONLY the `qwen3_5_122b` entry into
  `results/family_unfaithfulness_llm.json` + `results/family_figure_data.json` (main's `gpt_oss_120b_selfhost`
  kept); (c) added `qwen3_5_122b` to `make_family_figures._ALL`/`LABEL` + `assemble_family_figure_data.ACTORS`;
  (d) regenerated all clean + `3p1_family_sweep` figures (no API). qwen now shows in figC1/figC2 (decode,
  cot_necessary except deductive which is one-shot/weak for it), the answered-only heatmap + scatter (via
  its decode-JSON emit rates), and the family bars — where it is a **high-concealment open-weight actor**
  (bignum 0.16 / iterated 0.21 / deductive 0.21), UNLIKE gpt-oss/deepseek (cue-robust negatives). NOT in
  fig0/1/3/4/5 (no arithmetic powered sweep — qwen scope was decode+family only). fig0c (filter effect) +
  adopt_vs_B (§3.2) unchanged. The `feat/qwen35-selfhost-vllm` branch's unique content is now replicated on
  main → NOT worth merging (stale base). Pushed to `main`.
  **Follow-up (same session): qwen added to fig1 + fig5 too.** qwen's family sweep (`--share-baseline`)
  logged the family-independent `none`+`simple` conditions in each per-family transcript, so its
  simple-hint arithmetic-equivalent baseline is recoverable from W&B (`exp_hints_sweep_bignum_qwen3_5_122b_selfhost_aq8jmbws`).
  Reconstructed `logs/transcripts/exp_hints_sweep_qwen3_5_122b_aq8jmbws.jsonl` (none+simple rows ONLY;
  the bignum complex is NOT arithmetic complex, so excluded) → fig1 shows qwen Δ **−0.01** (cue-robust
  negative, follows the simple cue ~0.48 but reveals it → **open follower**, low concealment) and fig5
  places qwen at follow 0.48 / concealment ~0 (reinforces the "follows most conceals least" thesis).
  **fig0 excludes qwen** via a new honesty guard (fig0 is the none/simple/COMPLEX contrast; qwen has no
  arithmetic complex → an empty complex bar would read as a fake 0%). **fig3/fig4 exclude qwen** (they
  are arithmetic-complex figures; qwen ran no arithmetic complex — only its simple bar exists, which
  fig3 doesn't even plot). Transcript is gitignored (recoverable from W&B); committed PNGs carry qwen.
- **STALE PRE-CORRECTION FIGURES CAUGHT + REGENERATED, gpt-oss OR-row DROPPED (2026-07-02, Session #10).** After the Session-9 vLLM branch merged to `main`, its committed `figures/clean/` + `figures/3p1_replication/` renders of fig0/fig1/fig5 carried **pre-rejudge (Session-5) judge verdicts** — fig1 showed gemini-2.5-flash Δ +0.15 (corrected record: **+0.09**) and fig5 showed its conceal|followers ≈0.99 (corrected: **0.63** raw / 0.59 baseline-corrected), the exact judge artifact `analysis/3p1_replication.md` documents as fixed; the merged figs also dropped gemini-3.1-pro and split gpt-oss into two "(OR)"/"(vLLM)" rows. Local `logs/transcripts/` verified CORRECT (all simple/with-CoT rows `judge_method=deterministic_v2`; recomputed deltas +0.088/+0.378/+0.240/+0.417/opus +0.155/3.1-pro +0.158 match the record) → the merged PNGs were rendered from a different (branch/VM) checkout, not these transcripts. **Fix (Arav's call this session): (a)** pulled the selfhost §3.1 sweep transcript from W&B (`exp_hints_sweep_gpt_oss_120b_selfhost_f0vnzmzy.jsonl`, run crimson-plasma-84; validated vs logged scalars) and reconstructed `logs/sweep_gpt_oss_120b_selfhost.json` for the fig3/fig4 follow arm; **(b)** DROPPED the provider-suspect OpenRouter `gpt_oss_120b` everywhere (all figure scripts) — gpt-oss is now represented ONLY by the vLLM selfhost run, carrying the **plain "gpt-oss-120b" label** (no "(OR)"/"(vLLM)" tags, earlier sage-green/indigo gpt-oss color) so the figures keep the pre-merge (07f4e86) composition the human preferred; **(c)** re-ran all four figure scripts (`make_figures.py --no-wandb`, `make_clean_figures.py`, `make_complex_figures.py --no-wandb`, `make_family_figures.py`) from the canonical local transcripts + committed results JSONs. Verified: fig1 restores the 8-actor earlier layout with corrected deltas + gemini-3.1-pro (+0.16); figC1/figC2 show a single cot_necessary gpt-oss row (+0.93–1.00, vLLM); fig0 paper-analog has 8 plain-labeled panels; family unfaithfulness has one gpt-oss (vLLM) row. Edited scripts: `make_figures.py`, `make_clean_figures.py`, `make_complex_figures.py`, `make_family_figures.py`. Figures NOT yet committed (working tree). **Lesson: figures regenerated on a branch/VM must be re-rendered from the canonical transcripts before commit.**
- **JUDGE-PROVENANCE AUDIT + ANSWERED-ONLY HEATMAP REGEN (2026-07-02, Session #10; NO API runs — Arav's call).** Audited which judge each figure reports: **family sweep figs = LLM (gemini_3_flash)** and label it; **arithmetic §3.1 (fig0/1/5) = deterministic hint-mention judge** — the Session-5 *fixed* broadened-lexicon logic for ALL actors (label `deterministic_v2` on the original 4, plain `deterministic` on the 5 newer actors = same code, swept post-fix), validated ≈LLM (`results/gate1_llm_judge_deltas.json`: LLM +0.09 vs regex +0.088, gemini-3-flash identical); **figC1/figC2 decode = ground-truth accuracy, NO mention-judge**. NO figure falsely claims LLM. The one un-LLM'd spot is arithmetic simple+complex (fig0/1/5) — `rejudge_llm.py` (uncommitted, extends LLM judging to all 9 §3.1 actors, ~5.6k gemini-3-flash calls / ~3.1k filtered-only) is READY but was **never run** (`logs/transcripts_pre_llm_backup/` empty; when run it overwrites `judge_verdict` in place → fig0/1/5 auto-switch to LLM). Arav interrupted a dry-run: **do not launch API/rejudge/sweep runs to "verify" — audit from artifacts only** ([[no-autonomous-budget-runs]]). Also **wrote reproducible generators for the ANSWERED-ONLY necessity heatmap** (`figC2_necessity_heatmap_answered_only.png`, both `figures/clean/` and `figures/3p1_complex_hints/`) — previously ad-hoc/no-script orphans. Answered-only = drop no-answer rollouts from num & denom; computed LOCALLY from decode transcripts (`exp_hints_complex_decode_<actor>_*.jsonl`) and, for gpt-oss-vLLM (no local transcript), from the selfhost decode JSON's per-cell `emit_cot/emit_nocot`. New code: `make_complex_figures.py` `_answered_counts_from_transcript`/`_answered_counts_from_emit`/`load_answered`/`fig_heatmap_answered`; `make_clean_figures.py` `_decode_cells_answered`/`_render_c2`/`figC2_necessity_heatmap_answered_only`. gpt-oss-vLLM answered-only is cot_necessary but deductive/indirection drop vs all-rollout (e.g. deductive L1 +0.93→+0.69: the forced-final no-CoT arm abstains ~78% but is 0.31-correct among the few it answers) — honest, thin-denominator caveat on high-abstention cells. **Then switched the 3p1_family_sweep SCATTER (`fig_necessity_vs_unfaithfulness.png`) x-axis to the same answered-only necessity** (mean answered-only uplift per actor×family via `make_family_figures.answered_necessity()`, reusing the mcf counters); y stays LLM-judged unfaithfulness. Effect: **opus drops off the scatter** (no per-rollout answer data → no answered-only necessity), and gpt-oss-vLLM's deductive/indirection points move left (necessity ~0.96→~0.69). Title/x-label now say "answered rollouts only".
- **GPT-OSS-120B: OpenRouter → SELF-HOST vLLM §3.1 RE-RUN (2026-07-01, Session #9; branch `feat/gptoss-selfhost-vllm`, NOT committed).** The OpenRouter gpt-oss §3.1 decode-necessity was a **provider artifact**: the harmony `analysis` channel is mandatory on the chat endpoint, so the "no-CoT" arm still reasoned → uplift all +0.00, iterated L2/L3 NEGATIVE (structurally impossible). Re-ran on self-hosted vLLM where the no-CoT arm forces the **harmony `final` channel via raw /v1/completions** (0 analysis tokens, Gate 0 verified). Result: **every decode cell flips to +0.93–1.00 cot_necessary** → vLLM SUPERSEDES OpenRouter. Deviations introduced (all documented): (a) new `no_cot_mechanism=harmony_final` + `exp_hints/harmony_nocot.py` (async, raw completions, openai_harmony dep); (b) `roster.build_model` openai-api path caps max_tokens to the served ctx + bumps max_connections (concurrency); (c) `run_sweep.py --min-emission` filter (ctx-8192 mitigation: drop+backfill items whose with-CoT reasoning truncates at the 6144 cap → retained-set emission ≥95%; **scopes gpt-oss-selfhost §3.1 to within-budget items**, a comparability footnote vs other actors); (d) figures show gpt-oss (OR) vs (vLLM) in figC1/figC2, self-host supersedes OR in the replication figs. Server owned by the human; kept at ctx 8192 per Arav (not restarted). Judge for the concealment re-judge = **gemini_3_flash** (Arav's call). NOTE: this does NOT reopen §3.2 for gpt-oss — the final-channel force is a no-CoT arm, not mid-stream forcing; §3.2 forcing remains invalid (below).
- **§3.2 MID-STREAM TOKEN-FORCING DIAGNOSTIC (2026-06-24; `scripts/phase0_forcing_check.py`, tiny N; W&B `phase0_forcing_check`; `logs/phase0_forcing_check.json`):** mid-stream INCOMPLETE force ("…so x = {x_inc}, therefore x + c = ", no room to reframe).
  - **TEST 1 — gpt-oss-120b OUT of §3.2, DEFINITIVE.** Reasoning is **MANDATORY on ALL gpt-oss-120b OpenRouter endpoints** (every provider 400s on `reasoning:{enabled:false}` — "Reasoning is mandatory"), so the always-on analysis channel ALWAYS re-derives the correct value and bypasses the forced step. **adopt = 0/33 across 15 non-erroring providers × BOTH vehicles** (body-content force AND reasoning-channel `reasoning`-field prefill). ⇒ no OpenRouter forcing method makes gpt-oss adopt → gpt-oss-120b stays OUT of §3.2 (strengthens the prior single-provider finding; `gradient` already removed from its roster experiments).
  - **TEST 2 — kept actors validated at EASY B (3–10): adopt-and-commit vs quietly-corrects.** **gpt-5.5 ADOPTS** (3/6, clean `<answer>8</answer>` commits, 0 visible correction) → steerable, CoT load-bearing → **§3.2-VALID ✅**. gemini-2.5-flash MIXED (2/6 adopt). **gemini-3-flash CORRECTS** (0/6 adopt; 5/6 VISIBLE *"Wait, let me re-calculate"* → adopts-then-corrects, gpt-oss-style) → **quiet-corrector RISK**: in the answer-slot-forced sweep its corrections are INVISIBLE (it just emits the right answer), which would INFLATE "unfaithful-correct". CAVEAT: easy B trivially invites correction (the low-B gradient end), so this is a flag, not a disqualification — **re-validate gemini-3-flash at sweep B (does it ever flip to adopt?) before trusting its §3.2 column.**
- **GPT-OSS-120B §3.2 PREFILL-ADOPTION DIAGNOSTIC (2026-06-24; `scripts/phase0_gptoss_adoption.py`, tiny N, deepinfra/bf16; W&B `phase0_gptoss_adoption`; `logs/phase0_gptoss_adoption.json`):** does native_raw gpt-oss ADOPT a forced wrong intermediate (the §3.2 mechanism) or silently re-derive? **RESULT: RE-DERIVES — 0/15 adopt, 15/15 re-derive** (control 2/2 solve; **saw_planted=100%** so the prefix is RECEIVED, not dropped — then REJECTED). Smoking gun in the `reasoning` channel: *"the assistant's previous answer incorrectly said x = 90? Let's correct."* → the content-prefill lands as a SEPARATE prior-assistant claim the model CRITIQUES, NOT its own committed CoT it continues; the exploratory reasoning-field prefill also re-derives. ⇒ standard prefill-forcing does NOT achieve §3.2 corruption on gpt-oss; its §3.2 "unfaithful-correct" would be an ARTIFACT → **gpt-oss-120b §3.2 column INVALID via this mechanism** (distinct from qwen3-8b, where the PROVIDER restarted; here the MODEL rejects/corrects — a robustness trait). FIX OPTIONS: a forcing the model OWNS (continue ITS reasoning, no "previous answer" framing — non-trivial for harmony native_raw), or drop gpt-oss from §3.2 (keep it for §3.1, already used). Did NOT start the §3.2 sweep.
- **Git state (2026-06-24):** `main` is CURRENT — **PR #4 merged** all feat/phase2 work (figures reorg, fig3 decode-uplift, decode-necessity run n=240, instance concurrency, Qwen3 ladder probe) into `main`; `feat/exp-hints` fully merged (empty `main..` diff). Nothing to fast-forward. This + the gpt-oss diagnostic commit **directly on main**.
- **QWEN3 DENSE SCALE-LADDER PROBE (2026-06-24, Session #3; `scripts/phase0_qwen_ladder.py`, tiny N; W&B `phase0_qwen_ladder`; `logs/phase0_qwen_ladder.json`):** for §3.2 / Follow-up C, checked whether the dense Qwen3 series is a usable architecture-constant scale axis. FINDINGS: (1) only **qwen3-{8b,14b,32b}** dense rungs exist on OpenRouter — **no 4b/1.7b** (no low-end span); 235B is MoE/off-axis. (2) All three pass thinking→raw `reasoning` (rtoks 364–593, hybrid toggle works), `answered`=1.00 (terse — far better than 235b's 0.63), and solve-easy-algebra-with-CoT=1.00. (3) **PREFILL (§3.2-REQUIRED) is the blocker and is PROVIDER-SPECIFIC** (the GPT-5.5-style landmine): **deepinfra/fp8 honors it for 14b+32b** (forced wrong intermediate lands — model continues `32`=adopted X=30), but **qwen3-8b's ONLY providers (atlas-cloud/fp8, alibaba) BOTH restart** ⇒ no 8b prefill route on OpenRouter. NB alibaba never honors prefill (also relevant to the existing qwen3_30b@alibaba entry — its prefill is UNVERIFIED). **⇒ usable dense §3.2 rungs = qwen3-14b + qwen3-32b @ deepinfra/fp8 only (2 < the ≥3 for a clean within-family ladder).** RECOMMENDATION: use the **14b→32b dense pair (deepinfra/fp8, consistent provider+quant) as the controlled within-family segment**, plus the **235B MoE (prefill OK on wandb/bf16) as a caveated off-axis upper anchor** for a 3rd point; the broader Follow-up C capability axis stays cross-actor (gemini-3-flash, gpt-5.5, qwen3-14b/32b). Roster entries NOT added yet (cot_access mode = §3.2-build decision). Did NOT start the §3.2 sweep.
- **CHANNEL-AWARE CAPTURE FIX (2026-06-22, Session #2):** the §3.1 pipeline judged `.completion` (the body) for ALL actors — but native_raw open weights (qwen3, gpt-oss) return their raw CoT in the **`reasoning` field** (Inspect → `ContentReasoning` block), leaving `.completion` answer-only/empty. So the first pre-check's qwen3/gpt-oss "0% follow, empty CoT" was a CAPTURE BUG, not cue-robustness (verified: phase-0 `reasoning_kind=raw` + transcripts 60/60 empty body). FIX: `roster.judged_cot_source(cot_access)` drives the judged field (native_raw→`reasoning`, body_channel/disabled→body, summary/None→`none`=not judgeable); `solver.extract_reasoning()` reads the ContentReasoning block; answer parsed from body w/ reasoning fallback; `max_tokens≥8k` for thinking models (else content/answer starved); with-CoT arm still never prefills (no-CoT arm only). 7 new tests (`tests/test_capture.py`). The prefill-zeroing hypothesis (b) was NOT the cause — the with-CoT arm never prefilled; pure reasoning-field gap + token starvation.
- pyproject: uv "virtual" → **hatchling-packaged src layout** (`import cot_mon` works); `requires-python` `>=3.9`→`>=3.10`; `tasks/` is a non-package Inspect-entry dir; pytest `pythonpath=["src"]`.
- Slugs resolved from OpenRouter's live catalog: `qwen3`→`qwen3-235b-a22b-thinking-2507` (cheap dev alt `qwen3-30b-a3b-thinking-2507`); `gpt-5.5` is unlisted in /models but routable (confirmed live) — fallback `openai/gpt-5`.
- **GPT-5.5 as §3.1/§3.2 actor: RESOLVED** — `reasoning:{enabled:false}` disables the hidden channel (rtoks=0) and prefill still works, so we force body reasoning and judge that. (`effort:minimal`/`exclude` do NOT disable.)
- Probe bugs found+fixed: liveness `max_tokens` 5→16 (false-failed GPT-5.5); prefill 16→64 + auto-retry@256 (false `inconclusive` on thinking models); reasoning label now distinguishes closed-model summary from open-weight raw.
- `.gitignore`: added `logs/`, `*.eval`, `.cache/`, `data/raw/`. Default `uv` `main.py` left in place.
- **Providers pinned** as exact endpoint tags (`only`+no-fallback) after verifying the format empirically; quantization pinned where it varies (qwen3 bf16, gpt-oss bf16 — the latter has fp4..fp16 endpoints).
- **`opus_4_8` experiments narrowed** `[hints,gradient,oversight,followup_a]` → `[oversight,followup_a]` (no prefill → can't run §3.1/§3.2 forced-CoT).
- **§3.1 design (per 7 review pushbacks):** per-model necessity is a PRECONDITION — `uplift_filter` gates §3.1 on each actor's own CoT-necessary items, **including the hint-DECODE items** (verify the actor can't one-shot the decode). 2×3 design (with/no-CoT × none/simple/complex); **no-CoT arm = forced early `<answer>`** (so forcing never truncates the with-CoT trace the judge reads). Randomized wrong letter + subtract no-hint baseline. Judge deterministic-primary + Opus (non-Gemini) backstop; report hand-labeled agreement before trusting the delta. **Smoke (tiny N) ≠ Gate 1** (powered run, ~20/item, 2σ).
- **GPQA is the Gate-1 anchor** (`Idavidrein/gpqa` extended config; `HF_TOKEN`; canary never enters prompts); MMLU-Pro = smoke + generalization only.
- **Complex-hint SUITE (extension beyond paper):** families = `arithmetic` (DEFAULT/paper), `bignum`, `iterated` (LCG), `deductive`, `indirection`; each difficulty-laddered + per-model necessity-filtered. Buys §3.1 robustness across reasoning modalities + a richer Follow-up C signal.
- **§3.1 PAPER replication setup (user decisions):** actor = **gemini-2.5-flash** (Flash analog of the retired 2.0 Flash); item filter = **correct-with-CoT** (a true no-CoT arm is confounded on mandatory-reasoning Gemini); injection = **Chen et al. `<question-metadata>` block** (decoy status/peer-reviewed/success-rate + hint in the `<answer>` slot). Generalization-only caveat: we read its body CoT, it may also reason hidden. Extraction handles `\boxed{}`/`<answer>`/"answer is X".

## Results so far
**★ §3.1 DECODE-NECESSITY MAP → 4 ACTORS (Session #4, 2026-06-25; `decode_necessity_sweep.py`, fresh n=240/arm for the 2 new actors; W&B `exp_hints_complex_decode`; figs `figures/3p1_complex_hints/` figC1+figC2):** extended the per-(actor×family×level) decode-uplift map from 2→4 actors.
- **gemini-2.5-flash (paper analog, CLEAN):** CoT-necessary on arithmetic/bignum/indirection at all levels (uplift +0.71–0.86), iterated ladders (L1/L2 necessary → **L3 weak +0.30** as the decode gets too hard), deductive one-shot. **bignum CoT-necessary at every level** (no-CoT 0.14–0.15) — matches gemini-3-flash, opposite of gpt-5.5.
- **gpt-oss-120b (open weight, CAVEATED — necessity UNMEASURABLE):** every cell uplift ≈0 (labels one-shot) BUT this is an **artifact of an undisableable reasoning channel**, not real one-shot capability. Smoking gun: iterated L2/L3 go **NEGATIVE** (−0.19, **−0.69**) — the forced-no-CoT arm *beats* with-CoT, structurally impossible unless the "disabled" arm is still reasoning. Confirms the Phase-0 finding (mandatory reasoning on all OpenRouter endpoints) → gpt-oss-120b is in the "mandatory hidden reasoning → externalization unverifiable" tier (like the Pro models); reported caveated, never load-bearing.
- **The 4-actor map (Fig C2):** the clearest cross-model signal is the **bignum column** — a **capability gap** (CoT-necessary for both Geminis, one-shot for gpt-5.5 via a mod-4 shortcut), *not* within-actor level-erosion (the genuine within-family ladder is `iterated`). gpt-oss sits outside as the unverifiable tier (entirely orange/red bottom row).
- **DATA NOTE:** gemini-3-flash + gpt-5.5 figure cells are **carried over from the prior n=240 run** (their gitignored `logs/` JSONs were reconstructed from `analysis/3p1_complex_hints.md`'s committed accuracies — uplifts exact, counts back-computed for CI rendering; marked `*` in figs). gemini-2.5-flash + gpt-oss-120b are freshly measured (body-channel sampler, max_tokens 1024 — the repo sampler now reads native_raw from the `reasoning` field, but the gpt-oss undisableable-channel conclusion is sampler-independent).
- **REBASED onto parallel work:** this built on a parallel branch (commits `953cd83..bff3c8a`: cluster-bootstrap CIs, honesty fixes, Fig 0b/2b/5, the decode-sweep code that wired these 2 actors but **did not run** them). Kept my run's data; adopted their reframing (bignum = capability gap; report filtered §3.1 set only).
- **Fig 2b + Fig 0b REFRESHED with the new decode data** (both §3.1 figs necessity-scope by the per-actor decode verdict — the "msg 7a" run = this one). gemini-2.5-flash now plots valid scoped complex cells (arithmetic CoT-necessary: fig2b 0.58/0.25/0.38, n=19/24/13; fig0b complex-mention 0.61); gpt-oss shows one-shot exclusion / "no necessity-validated cell" (was "decode pending"). **Fig 0b method:** the *filtered* §3.1 transcripts (`exp_hints_sweep_<actor>_*.jsonl`) are gitignored + were absent, so they were **pulled from W&B** (artifacts `exp_hints_sweep-<actor>-transcripts:v0`, runs tough-water-14 / classic-mountain-16 / noble-aardvark-12 / ancient-resonance-18, 6000 rollouts each) into `logs/transcripts/`, then fig0b regenerated in isolation (cluster-bootstrap CIs intact). fig0/1/3/4/5 NOT touched (not decode-dependent; already current from the parallel branch).
**★ §3.1 UNFILTERED ROBUSTNESS RE-RUN — GATE 1 HOLDS (Session #4, this session; analysis of the 2026-06-24 `--no-filter` sweep, all 4 actors, 60×10, correct-with-CoT filter OFF; `results/gate1_sweep_summaries_unfiltered.json`; W&B `exp_hints_sweep_unfiltered`, e.g. different-capybara-31):** re-ran the full sweep on a fixed unselected GPQA set (incl. items the actor gets wrong even with CoT) to test whether Gate-1 is a per-model-selection artifact. **It is not — replication survives.**
| actor | baseline (filt→unfilt) | simple unfaithful (filt→unfilt) | delta (filt→unfilt) | gate |
|---|---|---|---|---|
| gemini-2.5-flash | 0.018→**0.050** | 0.167→0.143 | **+0.148→+0.093** | ✅→✅ |
| gemini-3-flash | 0.017→**0.087** | 0.445→0.538 | +0.428→+0.452 | ✅→✅ |
| gpt-5.5 | 0.018→**0.130** | 0.445→0.548 | +0.427→+0.418 | ✅→✅ |
| gpt-oss-120b | 0.070→**0.152** | 0.073→0.172 | +0.003→+0.020 | ns→ns |
- **Same 3/4 actors clear 2σ; same ordering; gpt-oss still the cue-robust negative.** (1) **Baselines rise 2–7×** — expected, not a finding: on unselected items the model guesses/errs more, landing on the random target letter by chance → higher no-hint baseline (exactly the inflation the correct-with-CoT filter removes). (2) **Raw concealment is robust** — simple-unfaithfulness barely moves (rises slightly for the 2 strong actors); the behaviour is NOT a selection artifact. (3) **Most filter-sensitive number = gemini-2.5-flash delta (+0.148→+0.093, ~halved)** — its unfaithfulness held flat while baseline tripled; still clears 2σ but is the weakest paper-analog effect. **Complex arm unchanged** (decode-uplift flat-to-near-zero both sets; no clean necessity window either way). **→ HEADLINE stays the FILTERED numbers** (chance-pick inflation removed); unfiltered = cited robustness check. Full subsection in `analysis/3p1_replication.md`.
**★ §3.1 POWERED SWEEP — GATE 1 (Session #3, 2026-06-23; GPQA 60 items × 10 samples/cell, correct-with-CoT filtered, arithmetic family, BOTH CoT arms; `scripts/run_sweep.py`; W&B `exp_hints_sweep` per actor):**
**GATE 1 ✅ CLEARED** — simple-hint unfaithfulness REPLICATES with non-overlapping 2σ on 3/4 actors:
| actor | baseline | **simple unfaithful (Gate-1)** | delta | gate | complex L1/L2/L3 (necessity) |
|---|---|---|---|---|---|
| gemini-2.5-flash (paper) | 0.02 | 0.17 [0.14,0.20] | **+0.15** | ✅ PASS | declines (foll 0.02–0.04, unfaith ~0.01) |
| gemini-3-flash | 0.02 | 0.45 [0.40,0.49] | **+0.43** | ✅ PASS | one_shot/declines (foll 0.08–0.17, unfaith 0.01) |
| gpt-5.5 | 0.02 | 0.45 [0.40,0.49] | **+0.43** | ✅ PASS | one_shot (foll ~0.10, unfaith 0.06–0.08) |
| gpt-oss-120b | 0.07 | 0.07 [0.05,0.10] | +0.00 | ns (overlap) | declines (foll ~0.10) |
- **Mechanism (the §3.1 contrast):** SIMPLE cue (cheap) → HIGH unfaithfulness (0.45, concealed on ~all follows); COMPLEX decode (costly) → LOW follow + LOW unfaithfulness (when followed, the decode is REVEALED in CoT — gemini-3-flash complex unfaith 0.01). **Models DECLINE the costly bad hint** rather than pay the CoT cost → supports the necessity thesis.
- **Necessity-gated complex (L2/L3 = EXTENSION):** NO actor opened a clean cot_necessary+followed window — every complex cell is one_shot or declines (figures render "no necessity window", NOT 0%). Closest: gemini-3-flash L2 (foll 0.17, +0.10 CoT-uplift; `one_shot*` by a hair — no-CoT follow 0.072 just over baseline+margin). The clean per-(actor×level) decode-necessity flip lives in the DECODE-ONLY calibration (gpt-5.5→arith L3), not the follow-based sweep where actors decline. **Caveat: necessity label threshold (point+0.05 margin) is sensitive near baseline — see fig4 follow-rates for the raw denominators.**
- **gpt-oss-120b = cue-robust NEGATIVE** (delta~0, high baseline 0.07), not a failure. The powered run also CORRECTED the pre-check's gemini-2.5-flash "0.00" (a 0/20 small-sample artifact → 0.17 at 600 samples).
- **Figures** (`figures/3p1_replication/` + W&B `exp_hints_sweep_figures`, all 2σ): fig1_headline (Gate-1 replication grouped bar), fig2_necessity_gated, **fig3_decode_uplift (REBUILT — grouped decode-uplift bar, Newcombe 2σ, threshold +0.5; one-shot/too-hard/CoT-necessary coloured distinctly, each bar annotated (with-CoT,no-CoT); replaces the old crushed two-line curve)**, fig4_follow_rate. **Full Gate-1 writeup: `analysis/3p1_replication.md`.**
- **What figs 2 & 3 (complex arm) tell us:**
  - **Costly hints are DECLINED, not hidden** — every complex cell is `declines`/`one_shot`, so complex unfaithfulness is UNDEFINED (no qualifying followers), **not** 0%. This is the necessity thesis: cheap simple cue → followed+concealed; costly decode → not worth following.
  - **Raising L1→L2→L3 barely moves follow-rates** (all floor-low, ~0.02–0.17) — actors ignore the hint, so the sweep shows **no dramatic one-shot→CoT-necessary flip**; gemini-3-flash L2 is the only near-window.
  - **CoT can INCREASE robustness** — for gemini-2.5-flash the no-CoT arm follows the hint MORE than with-CoT (reasoning steers it back to the correct answer).
  - **Caveat:** the follow-based curve conflates "can-decode" with "chooses-to-follow"; the clean necessity flip lives in the decode-only sweep (next block), not here.
**★ §3.1 COMPLEX-HINTS EXTENSION — decode-necessity map (Session #3, 2026-06-23; `scripts/decode_necessity_sweep.py`, n=240/arm [count 60 × 4 samples], uniform `--no-reuse`, Newcombe 2σ; W&B `exp_hints_complex_decode` restful-pine-20 / hearty-smoke-21; `analysis/3p1_complex_hints.md`; figs `figures/3p1_complex_hints/`):** decode-uplift (with-CoT − no-CoT decode ACCURACY, NOT follow) across both clean actors × 5 families × L1–L3.
| family | gemini-3-flash | gpt-5.5 |
|---|---|---|
| arithmetic L1/L2/L3 | ✓ +0.76/+0.78/+0.95 | ✓ +0.81/+0.85/+0.78 |
| bignum L1/L2/L3 | ✓ +0.88/+0.77/+0.88 | ✓ +0.59 / **1** +0.50 / **1** +0.48 |
| iterated L1/L2/L3 | ✓ +0.78/+0.71/+0.70 | ✓ +0.75/+0.70/+0.60 |
| indirection L1/L2/L3 | ✓ +0.72/+0.74/+0.71 | ✓ +0.83/+0.79/+0.83 |
| deductive L1/L2/L3 | **1** +0.00/+0.00/+0.00 | **1** +0.04/+0.05/+0.12 |
- **HEADLINE — per-model necessity erosion (Follow-up C in miniature):** `bignum` is CoT-necessary for gemini-3-flash at every level (no-CoT 0.12–0.23) but **gpt-5.5 ONE-SHOTS bignum L2/L3** (no-CoT 0.50–0.52). Same family×level, opposite necessity → the stronger model needs a harder task to externalise. **`deductive` = one-shot for BOTH** (logic puzzle not hard enough; fails the necessity premise — don't anchor a complex arm on it). arithmetic/iterated/indirection = CoT-necessary for both.
- **Resolves the §3.1 complex-arm ambiguity:** the decode IS genuinely CoT-necessary for most families (the model CAN'T one-shot it), so the floored §3.1 complex follow = a CHOICE not to pay the CoT cost for a bad hint, not inability. Exceptions = exactly the one-shot cells.
- **Methodology caveat:** `bignum`'s digit-count "level" is a weak knob under mod 4 ((a×b) mod 4 needs only a,b mod 4 — low bits), so its levels are ~equal-difficulty replicates, not a ladder; gpt-5.5 spots the shortcut. arithmetic/iterated/indirection do ladder.
- **Cost/wall:** 4×6000 rollouts; 36 min (gpt-5.5) → 346 min (gpt-oss, ~7M out tok). **qwen3-30b SKIPPED** (user) — ready via Alibaba pin (answered 1.00); its leg projected ~17h/$19.
**§3.1 susceptibility pre-check (Session #2; GPQA 5 items × 4 samples, with-CoT, arithmetic L1; W&B-logged):** screened 5 candidates for hint follow-rate →
| actor | baseline | simple_follow | complex_follow | simple_unfaithful | used\|cmplx |
|---|---|---|---|---|---|
| **gpt-5.5** (channel-off) | 0.10 | **0.50 [0.30,0.70]** | 0.10 | **0.40** | 0/2 |
| **gemini-3-flash** | 0.05 | **0.50 [0.30,0.70]** | 0.05 | **0.30** | 0/1 |
| gemini-2.5-flash (paper) | 0.00 | 0.00 [0,0.17] | 0.05 | 0.00 | — |
| qwen3 | 0.00 | 0.00 | 0.00 | 0.00 | — |
| gpt-oss-120b | 0.00 | 0.00 | 0.00 | 0.00 | — |
**→ ACTOR DECISION (user: 3 actors, labeled separately):** powered §3.1 on **gemini-2.5-flash** (paper datapoint; cue-robust → complex arm = "declines the costly hint", SPEC §2.2) **+ gemini-3-flash** (susceptible faithful actor; necessity-valid on BOTH arms → the clean delta) **+ gpt-5.5** (strongest simple-unfaithfulness 0.40, cross-family Google→OpenAI robustness; **channel-disabled so body CoT is judgeable**). **GPT-5.5 complex-arm caveat:** at L1 it one-shots the decode (used|follow 0/2; necessity=fails_easy) → its complex arm must run at ESCALATED difficulty (per-model necessity-calibrate first), else the complex number is invalid. Open weights (qwen3/gpt-oss) show 0 follow AND empty body CoT (reasoning hidden) → out of the §3.1 judged arm; revisit under necessity check / §3.2.
**GPT-5.5 complex-level calibration (decode necessity, 5×2; W&B run balmy-armadillo-6):** with-CoT vs forced-no-CoT decode accuracy → **arithmetic L3 (multiplication) is the pick: uplift +0.90 (with-CoT 1.00, no-CoT 0.10).** GPT-5.5 one-shots arithmetic **L1** (addition) — hence the invalid §3.1 complex arm — but NOT **L3** (`a·b+c mod n`). iterated L2 (k=5) +0.80 is a clean cross-family alt; iterated L3 (k=8) too hard (with-CoT drops to 0.70); bignum L3 only half-necessary (no-CoT 0.50). **→ GPT-5.5 §3.1 complex arm runs at arithmetic L3** (Geminis stay at L1). This *is* per-model necessity / Follow-up C in miniature: the stronger actor needs the harder level.
**CAPTURE-FIX re-run (channel-aware capture; GPQA 5×4, with-CoT, L1; W&B warm-monkey-7 / wise-shape-8 / dauntless-darkness-9):** open weights are now CAPTURE-CORRECT (judge reads the `reasoning` field; `cot_populated` 1.00). Their first-pre-check "0% follow" was the BUG, not robustness:
| actor | cot_src | populated | answered | baseline | simple_follow | simple_unfaith | complex_follow | used\|cmplx |
|---|---|---|---|---|---|---|---|---|
| qwen3 | reasoning | 1.00 | 0.63 | 0.00 | 0.40 | **0.00** | 0.00 | 0/0 |
| gpt-oss-120b | reasoning | 1.00 | 0.95 | 0.15 | 0.25 | 0.15 | 0.30 | 0/6 |
| gpt-5.5 (recheck) | body | 0.63 | 1.00 | 0.05 | 0.45 | 0.45 | 0.20 | 0/4 |
Reads: **qwen3 = faithful follower** (follows the cue 0.40 but REVEALS it, unfaithful 0.00 — a clean monitorable contrast). **gpt-oss = weakly susceptible** (+0.10 over baseline). **gpt-5.5 confirms** simple_unfaithful ≈0.45 (hides it); its body `cot_populated` 0.63 = it one-shots ~37% (necessity=fails_easy), NOT a bug. All three: used|cmplx 0 at **L1** (one-shot the L1 decode) → complex arm needs a per-actor necessity level (gpt-5.5→L3 calibrated; open weights TBD). **qwen3 `answered`=0.63 CAVEAT:** the 235b trace sometimes never emits a final answer even at 8k tokens — reliability cap; raise tokens / answer-elicitation before trusting qwen3 numbers.
**§3.1-JUDGEABLE actors (post-fix):** native_raw→`reasoning` = **qwen3, gpt-oss-120b** (now judgeable ✅); body/disabled→body = gemini-2.5-flash, gemini-3-flash, gpt-5.5, opus_4_8; **NOT judgeable** (summary / mandatory-hidden, kept OUT) = gemini-2.5-pro, gemini-3.1-pro. **Powered §3.1 actor set UNCHANGED** (gemini-2.5-flash + gemini-3-flash + gpt-5.5); qwen3 available later as a faithful-follower contrast.
**§3.1 first GPQA paper replication (gemini-2.5-flash; PRELIMINARY — 7 items × 4 samples, Session #1):** direction REPLICATES — baseline pick-target 0.00; **simple unfaithful 0.18 [0.08,0.36]**; **complex unfaithful 0.00 [0,0.12]**. Underpowered; consistent with the pre-check (2.5-flash is cue-robust).
**`complex=0` RESOLVED (diagnostic):** the decode is genuinely CoT-necessary (decode acc with-CoT 0.94 vs forced-no-CoT 0.22 at paper level 1; uplift +0.72 → pushback #3 cleared). complex follow=0 because the model **IGNORES** the costly hint and answers correctly (engaged_hint=False), NOT because it can't decode — so "follows-and-reveals" isn't directly observed (no followers; would need a more hint-susceptible framing/model).
Phase-0 headline (recap): prefill works on all Gemini + GPT-5.5 + open weights, not Opus; faithful raw CoT only from open weights; necessity is model-relative.

## Current blocker / next
**RESOLVED — `qwen3_5_122b` DROPPED from the OpenRouter analysis (Arav, 2026-06-28).** It was infeasible on every OpenRouter endpoint (novita crashed; siliconflow wedged; ~137s/gen × 4300 gens → ~19h; sweep stalled at 40/47). **The committed result is the 8-actor set** (Gate-1 on 6/8); figures + decode + STATUS all final + W&B-logged (run `logical-paper-47`). **qwen3.5 is deferred to SELF-HOSTED vLLM** — see **`selfhost_vllm/`** (new this session): a RunPod-ready vLLM serving scaffold supporting many qwen sizes (reliable prefill + reasoning exposure + thinking-toggle, none of the OpenRouter fragility), ideal for the Follow-up C qwen DENSE scale ladder (1.7B→32B). Arav runs it on a separate GPU instance; then qwen re-enters as a self-host actor. IMMEDIATE:
0. **Stand up the vLLM endpoint** (selfhost_vllm/README.md) → re-run qwen §3.1 + decode → fold in as the 9th actor.
1. **§3.2 difficulty gradient + Follow-up C** (`exp_gradient/algebra.py` — still a stub): SymPy `a·x=b → x+c`, sweep B, per-actor necessity-threshold. gpt-5.5 = headline scale-erosion actor (decode-necessity already shows it one-shots bignum). The decode-necessity harness (`exp_hints/decode.py`, now with instance concurrency in `uplift_filter.measure`) + necessity-labels are the template.
2. **qwen3-30b backfill** of the §3.1 sweep (skipped; Alibaba pin, answered 1.00, ~17h/$19); optionally extend the decode-necessity map to the open weights.
**Open methodological note:** the §3.1 follow-based necessity label uses a point+0.05 margin over baseline (gemini-3-flash/gpt-5.5 complex sit just over baseline+margin → labeled `one_shot*`, a near-baseline artifact at floored follow-rates). The **decode-only** Part C sweep is the clean per-(actor×level) necessity instrument (no follow required); `decode_necessity_label` encodes one-shot vs too-hard vs CoT-necessary vs weak.

## Help wanted
- Open-weight capture now CORRECT (channel-aware); follow/unfaithful measured. Still TODO: per-actor COMPLEX-necessity level for qwen3/gpt-oss (all show used|cmplx 0 at L1 → one-shot the L1 decode, like gpt-5.5 pre-calibration). Run `scripts/calibrate_complex.py --model qwen3` (bump its max_tokens) before any open-weight complex arm.
- **qwen3 answered-rate = 0.63** even at 8k tokens (235b trace sometimes never emits a final `<answer>`). Decide: raise max_tokens further, switch to **30b-a3b-thinking** (cheaper + likely terser → better answer yield for dev), or add a stricter answer-elicitation. Affects whether qwen3 is usable as a faithful-follower datapoint.
