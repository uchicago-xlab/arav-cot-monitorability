"""§3.2 difficulty gradient — SPEC §4.2.

Forced wrong-intermediate prefix on a SymPy algebra task; sweep difficulty B;
categorize {unfaithful-correct, consistent-incorrect, other, correct-with-
ordinary-CoT (control)}. Expect a flip from unfaithful-correct (easy) ->
consistent-incorrect (hard). Prefill required — confirm per model.

Follow-up C lives here: compare the flip point across model strengths.
Entry: tasks/exp_gradient.py.
"""
