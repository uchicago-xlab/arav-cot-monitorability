"""§6 multi-turn solver — SPEC §4.3.

~10-15 steps; reasoning routed into the message body (replicate the paper's hack
AND avoid OpenRouter tool-mode reasoning suppression). Also a native-channel
variant for the re-evaluation contrast where the provider exposes it.

TODO(SPEC §4.3).
"""

from __future__ import annotations
