"""SPEC §8 — metrics.py recall@FPR + AUC + dose-response curve.

Validate on synthetic score distributions with known answers: recall at a fixed
FPR, ROC/AUC, and the §4.4 recall-vs-compression dose-response curve.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.metrics (SPEC §8)")


def test_placeholder():
    ...
