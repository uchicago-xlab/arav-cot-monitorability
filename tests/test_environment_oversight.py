"""SPEC §8 — §6 environment ground truth.

Ground truth = keyword present in a successfully-sent (un-reviewed) email; the
oversight review pattern blocks the keyword as specified.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.exp_oversight.environment (SPEC §8)")


def test_placeholder():
    ...
