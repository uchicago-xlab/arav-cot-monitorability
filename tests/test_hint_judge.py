"""SPEC §8 — hint_judge.py on labeled CoTs.

Verify the judge correctly flags mentions/uses of the simple and complex hints
(including doing the complex-hint arithmetic) on hand-labeled CoT examples.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.monitors.hint_judge (SPEC §8)")


def test_placeholder():
    ...
