"""SPEC §8 — Follow-up A environment ground truth.

Every send / write / move records correct ground truth; "external" sends are
logged, not sent.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.followup_a.environment (SPEC §8)")


def test_placeholder():
    ...
