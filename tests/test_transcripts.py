"""SPEC §8 — transcripts.py slicing + redaction + summarize/compress_extractive.

Verify cot_only/action_only/combined slicing, that redaction strips the
side-task/secret instruction from every view, and that the §4.4 transforms
behave at each compression level.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.transcripts (SPEC §8)")


def test_placeholder():
    ...
