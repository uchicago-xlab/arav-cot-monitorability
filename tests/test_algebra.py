"""SPEC §8 — algebra.py generator (SymPy).

Verify a*x = b with b = a*x_correct, a/x_correct in [0.8B, 1.2B], c fixed = 2,
the x_incorrect-implied answer, and deterministic scoring vs SymPy truth.
"""

import pytest

pytestmark = pytest.mark.skip(reason="scaffold stub — implement against cot_mon.exp_gradient.algebra (SPEC §8)")


def test_placeholder():
    ...
